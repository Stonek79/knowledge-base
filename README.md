# Knowledge Base

Современная система управления документами с поддержкой поиска, конвертации и управления доступом.

## 🚀 Быстрый запуск (Dev-окружение)

### Предварительные требования
- Docker и Docker Compose
- `make`
- `pnpm`

### 1. Клонирование репозитория
```bash
git clone <repository-url>
cd knowledge-base
```

### 2. Настройка переменных окружения
```bash
# Создать .env.local из примера
cp .env.example .env.local
```
Убедитесь, что `.env.local` содержит правильные переменные. Для старта dev-среды в Docker критически важны `REDIS_URL` и `DATABASE_URL`, указывающие на имена сервисов.

**Пример `.env.local`:**
```env
# База данных (для подключения из приложения)
DATABASE_URL="postgresql://postgres:dev_password@postgres:5432/knowledge_base"

# Redis (для подключения из приложения и воркера)
REDIS_URL="redis://:dev_redis_password@redis:6379"

# MinIO
MINIO_ENDPOINT=minio
MINIO_PORT=9000
MINIO_ROOT_USER=kb_admin
MINIO_ROOT_PASSWORD=dev_minio_password
```

### 3. Запуск в Docker
Эта команда поднимет все необходимые сервисы (Postgres, Redis, MinIO и т.д.) и запустит приложение с воркером в режиме разработки с автоматической пересборкой.
```bash
make dev-up
```

### 4. Доступ к сервисам
- **Приложение:** http://localhost:3000
- **PostgreSQL (с хост-машины):** localhost:5435
- **MinIO Web UI:** http://localhost:9001
- **Redis (с хост-машины):** localhost:6379

---

## 🛠️ Разработка

Основной способ разработки — через `make dev-up`, как описано выше. Это гарантирует, что у вас запущено полное окружение.

Команды `pnpm dev`, `pnpm build:worker` и т.д. можно использовать для специфических задач, но они требуют, чтобы все зависимые сервисы (БД, Redis) уже были запущены.

### Работа с базой данных
Для выполнения команд миграции и сидинга в dev-окружении используйте `Makefile`:
```bash
# Применить миграции
make migrate-dev

# Заполнить базу начальными данными
make seed-dev

# Выполнить оба шага
make init-dev
```

---

## ⚙️ Качество кода и CI/CD

Для поддержания высокого качества кода в проекте используются автоматизированные инструменты проверки и интеграции.

### Линтинг и форматирование
- **Инструмент:** [Biome](https://biomejs.dev/) используется для форматирования, линтинга и проверки кода.
- **Команды:**
  - `pnpm lint` — проверяет код на ошибки.
  - `pnpm format` — форматирует весь код в проекте.
  - `pnpm check` — запускает все проверки (форматирование, линтинг) и автоматически исправляет ошибки.

### Pre-commit хуки
- **Инструменты:** `husky` и `lint-staged`.
- **Процесс:** Перед каждым коммитом автоматически запускается проверка и форматирование измененных (`staged`) файлов. Это предотвращает попадание в репозиторий кода с ошибками или неверным стилем.

### CI/CD
- **Инструмент:** GitHub Actions.
- **Процесс:** При каждом `push` или `pull request` в ветку `main` запускается рабочий процесс (`.github/workflows/ci.yml`), который выполняет:
  1. Установку зависимостей.
  2. Проверку линтером (`pnpm lint`).
  3. Сборку проекта (`pnpm build`), включая проверку типов TypeScript.
- **Цель:** Гарантировать, что основная ветка всегда находится в рабочем состоянии.

---

## 🔧 Конфигурация

### Настройки системы
- Лимит размера файла: 2MB (настраивается)
- Максимум файлов за загрузку: 10
- Поддерживаемые форматы: PDF, DOCX, XLSX, PPTX
- Автоматическая конвертация Office → PDF

### Переменные окружения
Ключевые переменные окружения для работы приложения (задаются в `.env.local` для разработки и в `.env.prod` для production):
```bash
# База данных
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE"

# MinIO
MINIO_ENDPOINT=minio
MINIO_PORT=9000
MINIO_ROOT_USER=user
MINIO_ROOT_PASSWORD=password

# Redis
REDIS_URL="redis://:PASSWORD@HOST:PORT"

# Gotenberg
GOTENBERG_URL=http://gotenberg:3000
```

---

## 📚 Документация

- [Руководство по развертыванию](./DEPLOYMENT_GUIDE.md) - Пошаговая инструкция по установке на production-сервер.
- **Документация API**:
  - Интерактивная документация доступна на странице `/admin/api-docs` после запуска приложения.
  - **Технологии**:
    - **Генерация спецификации**: `@asteasolutions/zod-to-openapi` используется для генерации спецификации OpenAPI 3.1 напрямую из Zod-схем.
    - **Отображение (UI)**: `@scalar/api-reference-react` рендерит интерактивный интерфейс документации.
  - **Архитектура**:
    - Описания эндпоинтов декомпозированы по ресурсам и находятся в `src/lib/openapi/`.
    - `OpenAPIService` (`src/lib/services/OpenAPIService.ts`) собирает все части в единую спецификацию.
    - Этот подход гарантирует, что документация всегда соответствует реальным Zod-схемам, используемым для валидации.

---

## 🚀 Production (Full Docker)

Этот метод развертывания предполагает, что Docker-образ приложения уже собран и загружен в Docker Registry (например, Docker Hub). Продакшен-сервер только скачивает и запускает готовый образ.

### 1. Настройка сервера (выполняется один раз)

1.  Склонируйте репозиторий на сервер.
2.  Создайте файл `.env.prod` (можно скопировать `.env.example`) и заполните его вашими данными.
3.  При необходимости настройте Nginx для проксирования запросов на порт приложения (например, 3000).

### 2. Процесс обновления

Для управления продакшен-средой используйте готовые команды из `Makefile`. Перед обновлением убедитесь, что новая версия Docker-образа уже собрана и загружена в ваш registry.

**На сервере, в папке проекта, выполните:**

```bash
# Основная команда: скачивает новый образ и перезапускает сервисы (БЕЗ миграций)
make prod-update

# Если вместе с обновлением нужно применить миграции базы данных
make prod-update-migrate
```

### Полезные команды для управления продакшеном

- `make help`: Показать список всех доступных команд и их описание.
- `make prod-logs`: Смотреть логи всех запущенных сервисов.
- `make prod-down`: Остановить все продакшен-сервисы.
- `make docker-prune`: Очистить сервер от старых и неиспользуемых Docker-образов.

---

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте feature branch
3. Внесите изменения
4. Добавьте тесты
5. Создайте Pull Request

## 📄 Лицензия

MIT License - см. [LICENSE](LICENSE) файл для деталей.