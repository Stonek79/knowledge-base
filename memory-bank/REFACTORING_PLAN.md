# План Рефакторинга и Внедрения Комплексной Системы Тестирования

Этот документ описывает стратегию безопасного перехода к архитектуре с внедрением зависимостей (DI) и построения полной пирамиды тестирования.

**Принципы:**
1.  **Expand-Contract (Parallel Change):** Сначала создаем новое, проверяем тестами, переключаем использование, удаляем старое. Приложение всегда остается рабочим.
2.  **Test-First (по возможности):** Пишем тесты для новой реализации до или во время ее написания.
3.  **CI/CD Integration:** Тесты должны автоматически запускаться в GitHub Actions и (частично) в pre-commit хуках.

---

## 🎯 Цели
1.  **Пирамида тестирования:**
    *   **Unit:** Jest (бизнес-логика, сервисы).
    *   **Integration:** Jest + Supertest + Docker (API роуты, БД).
    *   **E2E:** Playwright (пользовательские сценарии).
2.  **Архитектура:** Переход на DI для обеспечения тестируемости.
3.  **Автоматизация:** Исправление CI пайплайна и настройка хуков.

---

## 🛠 Этап 1: Фундамент Тестирования (Infrastructure)

### 1.1. Установка и Конфигурация (Jest)
- [x] Установить: `jest`, `ts-jest`, `@types/jest`, `jest-mock-extended`.
- [x] Конфиг `jest.config.ts`:
    - [x] Поддержка алиасов (`@/*`).
    - [x] Игнорирование E2E тестов (они для Playwright).
- [x] Скрипты в `package.json`:
    - [x] `test`: запуск unit-тестов.
    - [x] `test:watch`: режим разработки.
    - [ ] `test:ci`: запуск с coverage (нужно добавить/настроить).

### 1.2. Настройка Playwright (E2E)
- [x] Установить: `@playwright/test`.
- [x] Инициализировать конфиг (`playwright.config.ts` настроен).
- [x] Скрипт `test:e2e`.

### 1.3. Настройка CI/CD и Хуков
- [ ] **GitHub Actions (`.github/workflows/ci.yml`):**
    - [ ] Раскомментировать/добавить шаг `Run Tests` (частично сделано, но требует доработки).
    - [ ] Добавить сервис Postgres для интеграционных тестов.
    - [ ] Добавить шаг для отчетности по покрытию кода (coverage reporting).
    - [ ] Добавить проверку порогов покрытия (coverage threshold check).
- [ ] **Husky (`.husky/pre-commit`):**
    - [ ] Добавить запуск `pnpm test` (только unit-тесты для скорости) (требует включения тестов).

---

## 🏗 Этап 2: Безопасный Рефакторинг (Expand-Contract)

Мы будем двигаться снизу вверх: Репозитории -> Сервисы -> API.

### 2.1. Репозитории (Parallel Change)
1.  **Expand:**
    - [x] Создать *новый* класс `DocumentRepositoryV2` (или просто `DocumentRepository` как класс, если имя не занято, а старый переименовать в `Legacy...`).
    - [x] Он принимает `PrismaClient` в конструктор.
    - [x] Реализует методы как инстанс-метомы.
2.  **Test:**
    - [x] Написать Unit/Integration тесты для нового репозитория (базовые тесты для `DocumentRepositoryV2` существуют, но нужно расширить).
    - [ ] **Добавить тесты для `DocumentRepositoryV2`:** `findUnique()`, `update()`, `delete()`, методы для работы с транзакциями, методы поиска с фильтрацией (Приоритет 2).
3.  **Contract:** (Пока не удаляем старый, так как он используется в старых сервисах).

### 2.2. Сервисы (Parallel Change)
1.  **Expand:**
    - [x] Создать *новый* класс `DocumentService` (вместо старого `DocumentCommandService` со статикой).
    - [x] Принимает `DocumentRepositoryV2` в конструктор.
2.  **Test:**
    - [x] Написать Unit-тесты для `DocumentService`, мокая репозиторий (базовые тесты для `DocumentServiceV2` существуют, но нужно расширить).
    - [ ] **Покрыть `AuditLogServiceV2`:** `log()`, `getLogs()`, `pruneOldLogs()` (Приоритет 1).
    - [ ] **Добавить тесты для `DocumentQueryServiceV2`:** проверка фильтрации, пагинации, сортировки (Приоритет 2).
    - [ ] **Расширить покрытие методов `DocumentServiceV2`:** `restoreDocument` (Приоритет 2).
    - [ ] **Добавить тесты для `DocumentComposeService`** (Приоритет 2).
    - [ ] **Добавить тесты для `CleanupService` и `FileStorageService`** (Приоритет 2).
3.  **Wiring:**
    - [x] Создать контейнер (`src/lib/container.ts`), где собираются инстансы:
        ```typescript
        const prisma = new PrismaClient();
        const docRepo = new DocumentRepositoryV2(prisma);
        export const documentService = new DocumentService(docRepo);
        ```

### 2.3. Переключение API (Migration)
1.  [x] В `src/app/api/documents/route.ts` заменить вызовы старого статического сервиса на импортированный `documentService` (Все роуты переключены).
2.  [x] Проверить работу вручную и (если есть) E2E тестами.

### 2.4. Очистка (Cleanup)
1.  [x] Удалить старые статические классы (`DocumentCommandService`, старый репозиторий), когда на них не останется ссылок (Legacy удален).

---

## 🧪 Этап 3: Расширение Покрытия

### 3.1. Integration Tests
- [ ] **Добавить Integration тесты для API:**
    - [ ] `POST /api/documents` (создание)
    - [ ] `GET /api/documents` (список с фильтрами)
    - [ ] `PATCH /api/documents/:id` (обновление)
    - [ ] `DELETE /api/documents/:id` (удаление)
- [ ] Написать тесты для API роутов (`supertest`), поднимая тестовую БД в Docker.
- [ ] Проверить цепочку "API -> Service -> DB".

### 3.2. E2E Tests
- [x] Реализовать базовые сценарии: "Успешный логин", "Неверные учетные данные", "Отображение списка документов", "Загрузка нового документа".
- [ ] **Расширить E2E сценарии:** Полный цикл: Login → Upload → Search → Download → Delete. Проверка прав доступа в UI. Работа с категориями (Приоритет 2).

---

## 📋 План выполнения (Checklist)

- [x] **Infra**: Установить Jest, Playwright, обновить `package.json`.
- [x] **Infra**: Настроить `jest.config.ts` и `playwright.config.ts`.
- [ ] **CI**: Обновить `.github/workflows/ci.yml` (добавить Postgres service, включить тесты, покрытие, пороги).
- [ ] **Hooks**: Обновить `.husky/pre-commit` (добавить запуск тестов).
- [x] **Refactor**: Создать `DocumentRepository` (Class).
- [x] **Refactor**: Создать `DocumentService` (Class + DI).
- [x] **Test**: Unit-тесты для `DocumentService` (базовые есть, нужно расширить).
- [x] **Switch**: Переключить `/api/documents` на новый сервис.
- [x] **Verify**: Ручная проверка загрузки.
- [x] **Cleanup**: Удалить старый код (после полной миграции).
- [ ] **Test**: Добавить Integration тесты для API (Приоритет 1).
- [ ] **Test**: Покрыть `AuditLogServiceV2` (Приоритет 1).
- [ ] **Test**: Расширить покрытие `DocumentRepositoryV2` (Приоритет 2).
- [ ] **Test**: Добавить тесты для `DocumentQueryServiceV2`, `DocumentComposeService`, `CleanupService`, `FileStorageService` (Приоритет 2).
- [ ] **Test**: Расширить E2E сценарии (Приоритет 2).
- [ ] **Config**: Добавить Coverage отчеты в `jest.config.ts` (Приоритет 3).
- [ ] **Docs**: Документировать стратегию тестирования (обновить `memory-bank`, создать `TESTING_GUIDE.md`) (Приоритет 3).
